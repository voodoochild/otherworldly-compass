import React from 'react';

const HomeNav = () => {
    return (
        <nav className="homeNav">
            <button>Start a campaign</button>
            <button>Continue a campaign</button>
        </nav>
    );
};

export default HomeNav;
